import React, {Component} from 'react';

class About extends Component {
  render(){
    return (
      <div style={{fontFamily: `Spectral SC`, color: `white`, textAlign: `center`}}>
        Full stack developer specializing in Javascript, React, and Ruby on Rails. 
      </div>
    )
  }
}

export default About;
